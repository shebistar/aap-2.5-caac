gateway_validate_certs: false
gateway_username: "admin"
gateway_password: "<REPLACE>"
gateway_hostname: "<REPLACE>"
gateway_request_timeout: 900

export_platform_dir: platform_config_export

gateway_configs_dir: "clusters/prod"